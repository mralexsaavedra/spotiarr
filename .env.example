# ===========================================
# SpotiArr Configuration
# ===========================================

# Spotify API Credentials (REQUIRED)
# Get yours at: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret

# Callback URL (REQUIRED)
# 1. Local Development: http://127.0.0.1:3000/api/auth/spotify/callback
# 2. Production (with Traefik): https://YOUR_SERVER_IP/api/auth/spotify/callback
#    (Note: Traefik handles HTTPS on port 443, so no :3000 needed in URI)
SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/api/auth/spotify/callback

# User and Group ID for Docker (Optional, default: 1000)
# Match these with your host user for proper file permissions
PUID=1000
PGID=1000

# Docker Volume Mapping (REQUIRED)
# Absolute path on your host machine where tracks will be saved
DOWNLOADS=/path/to/your/downloads/folder

# Database config (REQUIRED for Local Dev, Optional override for Docker)
DATABASE_URL="file:./config/db.sqlite"

# Advanced Redis (Optional)
# REDIS_HOST=redis
# REDIS_PORT=6379