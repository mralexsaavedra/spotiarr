# ===========================================
# SpotiArr Configuration
# ===========================================

# Spotify API Credentials (REQUIRED)
# Get yours at: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret

# Spotify OAuth Redirect URI (OPTIONAL - Auto-generated from PUBLIC_HOST)
# SPOTIFY_REDIRECT_URI=http://localhost:5173/api/auth/spotify/callback

# Public hostname or IP address
# This is used to construct the full URL for OAuth callbacks
# 
# Development (automatic):
#   - Uses localhost by default
#   - Protocol: HTTP
#   - Port: 5173 (Vite dev server)
#   - Final URL: http://localhost:5173
#
# Production (automatic):
#   - Set to your server IP or domain (e.g., X.X.X.X or spotiarr.yourdomain.com)
#   - Protocol: HTTPS (if certificates exist)
#   - Port: 3000
#   - Final URL: https://your-ip:3000
PUBLIC_HOST=localhost

# YouTube Settings
# YT_COOKIES: Optional - for age-restricted content (export from browser)
# YT_COOKIES=

# Paths (relative to project root, local development only)
# In Docker, DOWNLOADS_PATH is set to /downloads by default
# DOWNLOADS_PATH=./downloads

# Redis Configuration
# For docker-compose: REDIS_HOST=redis
# For local development: REDIS_HOST=localhost
REDIS_HOST=redis
REDIS_PORT=6379

# Database
# Default is set in Dockerfile: file:/spotiarr/config/db.sqlite
# For local development, uncomment and use absolute path:
# DATABASE_URL="file:/absolute/path/to/your/project/config/db.sqlite"
