# ===========================================
# SpotiArr Configuration
# ===========================================

# Spotify API Credentials (REQUIRED)
# Get yours at: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id
SPOTIFY_CLIENT_SECRET=your_client_secret

# Spotify OAuth Redirect URI (OPTIONAL - Auto-generated from BASE_URL)
# SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/api/auth/spotify/callback

# Base URL - Public URL for OAuth callbacks
# This is the URL Spotify will redirect to after authentication
# 
# Local Development:
#   - Backend runs on port 3000, but Vite proxies it on port 5173
#   - Use http://localhost:5173 (Vite's dev server)
#   - Spotify will redirect to this URL after login
#
# Production/Docker:
#   - Use your public URL: https://your-domain.com or https://IP:PORT
#   - Must match the redirect URI in Spotify Developer Dashboard
BASE_URL=http://localhost:5173

# YouTube Settings
# YT_COOKIES: Optional - for age-restricted content (export from browser)
# YT_COOKIES=

# Paths (relative to project root, local development only)
# In Docker, DOWNLOADS_PATH is set to /downloads by default
# DOWNLOADS_PATH=./downloads

# Redis Configuration
# For docker-compose: REDIS_HOST=redis
# For local development: REDIS_HOST=localhost
REDIS_HOST=redis
REDIS_PORT=6379

# Database
# Default is set in Dockerfile: file:/spotiarr/config/db.sqlite
# For local development, uncomment and use absolute path:
# DATABASE_URL="file:/absolute/path/to/your/project/config/db.sqlite"
