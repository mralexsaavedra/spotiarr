export enum Path {
  HOME = "/",
  PLAYLIST_DETAIL = "/playlist/:id",
  PLAYLIST_PREVIEW = "/preview",
  HISTORY = "/history",
  SETTINGS = "/settings",
  RELEASES = "/releases",
  ARTISTS = "/artists",
  ARTIST_DETAIL = "/artist/:id",
  MY_PLAYLISTS = "/my-playlists",
}
