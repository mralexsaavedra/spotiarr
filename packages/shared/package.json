{
  "name": "@spotiarr/shared",
  "version": "0.0.0",
  "description": "Shared types and utilities for SpotiArr",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "oxlint src --fix",
    "format": "prettier --write \"src/**/*.{ts,json}\""
  },
  "devDependencies": {
    "@spotiarr/tsconfig": "workspace:*"
  }
}
